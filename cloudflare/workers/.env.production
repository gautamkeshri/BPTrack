# Production Environment Variables for Cloudflare Workers
# These are set as environment variables in the Cloudflare dashboard

# Allowed CORS origins (comma-separated)
# Add your Cloudflare Pages domain once deployed
ALLOWED_ORIGINS=https://bptrack.pages.dev,https://bptrack-*.pages.dev

# Session secret (used for session encryption)
SESSION_SECRET=change-this-to-a-secure-random-string-in-production
