# Blood Pressure Monitoring Application - Environment Configuration
# Copy this file to .env and configure your settings

# Environment Mode (development or production)
NODE_ENV=development

# MySQL Database Configuration (required for production)
# DATABASE_URL=mysql://username:password@localhost:3306/blood_pressure_app
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD='Radiall14#'
DB_NAME=blood_pressure_app

# Session Configuration (change in production)
SESSION_SECRET=your-secret-key-change-this-in-production

# Server Configuration
PORT=6060
HOST=0.0.0.0

# Development vs Production Notes:
# - Development mode uses in-memory storage (no database required)
# - Production mode requires MySQL database connection
# - Set NODE_ENV=production to use MySQL storage